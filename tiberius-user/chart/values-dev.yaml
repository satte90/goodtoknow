# THIS IS THE DEFAULT VALUE FILE
# Values here are passed to the sub-chart named springfield-springboot
springfield-springboot:
  springProfilesActive:
    - "dev"
  ingress:
    app:
      rewriteTarget: /
      hosts:
        "k8slb03t.ddc.teliasonera.net":
          - path: /dev/tiberius/user
